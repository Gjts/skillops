# Product RC verification record

> Version: v0.3.2-rc.1
> Recorded: 2026-07-25; audit amendment rerun: 2026-07-27
> Candidate code commit: `0b703c6dcbbc6841a70c3a290ffae928abda85b8`
> Audit-corrected candidate binding: the Git commit containing this amendment;
> its exact SHA and CI run are appended after push
> Pre-commit evidence state: local reruns below were captured before that
> immutable candidate existed
> Historical evidence source: uncommitted working tree based on
> `446ea71595c0e358abe35d8c4ec9d91ec9089968`; equivalence to the candidate was
> not recorded
> Decision: **Limited Preview accepted as this delivery endpoint; no-ship for general RC promotion**

This record contains synthetic fixture results and sanitized QA summaries only.
It contains no user event, prompt, transcript, credential, source path, raw
provider output, or runtime configuration value.

## Automated verification

| Environment | Result |
| --- | --- |
| Candidate commit GitHub Actions | [Run 30145860716](https://github.com/Gjts/skillops/actions/runs/30145860716) passed at `0b703c6dcbbc6841a70c3a290ffae928abda85b8`: Windows, Ubuntu, and macOS on Node `22.22.0`, plus Ubuntu on Node 24. Each job ran dependency installation, tests, build, artifact check, documentation check, production smoke, and the critical audit gate |
| Latest pushed `main` baseline | [Run 30191086426](https://github.com/Gjts/skillops/actions/runs/30191086426) at `ad367a1fa9d520efc07900e52c732416ab2c680e` passed install, tests, build, artifact, documentation, and smoke on all four jobs but failed the audit step. The dependency mitigation below remains uncommitted and therefore is not evidence for that pushed baseline |
| Audit-corrected local Windows 11 `10.0.26200`, Node `24.18.0`, npm `11.5.2`, Git `2.52.0.windows.1` | A clean default `npm ci` installed the final dependency tree with zero vulnerabilities. The audit-corrected pre-commit candidate tree then passed 78 test files with 800 tests passed and one platform-specific case skipped. Build, production smoke, documentation links, sanitized artifact check, adapter dry-runs, and the all-severity audit gate passed. The final deterministic 100k/5k endpoint and 30-minute memory protocol also passed. The installed runtimes are Codex CLI `0.145.0` and Claude Code `2.1.218` |
| Audit-corrected browser rerun, Chromium/Chrome `150.0.0.0` | The eight canonical routes passed direct-load and refresh checks at 390 and 1366 CSS px; seven legacy mappings also resolved to the intended page, for 30 route scenarios with no document overflow or console error. Axe-core `4.10.3` then covered the eight canonical routes at 390, 768, and 1366 CSS px plus a 683 px 200%-zoom equivalent: all 32 scenarios had zero Critical/Serious findings and zero findings overall. Browser-originated requests remained bounded. This is current local automated evidence, not independent keyboard or human accessibility sign-off |
| Historical local Windows 11 `10.0.26200`, Node `22.22.0` and `24.18.0` | On the pre-commit working tree, each version passed 72 test files with 596 tests passed and one platform-specific case skipped; build and production smoke passed; the Node 24 artifact check passed |
| Historical Linux Node `22.22.0` container | On the pre-commit working tree, 72 test files and 597 tests passed; build, artifact check, documentation links, production smoke, and critical audit gate passed |
| Historical Linux Node 24 container | On the pre-commit working tree, 72 test files and 597 tests passed; build, artifact check, documentation links, production smoke, and critical audit gate passed |

### Current working-tree dependency mitigation rerun

On 2026-07-26, after applying compatible transitive dependency overrides and
normalizing the lock file with npm `10.9.4`, the current uncommitted working
tree produced the following additional evidence:

- Windows Node `24.18.0`, npm `11.5.2`: 78 test files passed with 799 tests
  passed and one POSIX-only case skipped; build, documentation links, sanitized
  artifact validation, and production smoke passed.
- Debian Linux Node `22.22.0`, npm `10.9.4`: a fresh default `npm ci` installed
  771 packages; all four Promptfoo files passed 20 tests, the production build
  passed, and `npm audit --omit=dev` reported zero vulnerabilities.
- Debian Linux Node `24.18.0`, npm `11.16.0`: a fresh default `npm ci` installed
  768 packages; the same four files passed 20 tests, the production build
  passed, and `npm audit --omit=dev` reported zero vulnerabilities. npm 11 also
  emitted its informational pending-build-script approval notice for five
  transitive packages.

The compatible overrides reduce the audit report from 20 findings to zero
vulnerabilities. The final scoped override replaces only Promptfoo's exact
`js-yaml@5.2.1` dependency with `5.2.2`, while preserving its nested schema
parser on `js-yaml@4.3.0`; all four focused Promptfoo files and the full suite
pass. Details are in the
[Promptfoo integration contract](../../integrations/promptfoo.md#dependency-validation).
This evidence remains working-tree evidence and does not close the immutable
candidate, macOS, independent-reviewer, or human Golden Path gates.

The historical local Windows skip is the POSIX executable-mode drift scenario;
that same test passed in the historical Linux matrices. The base candidate
commit's four GitHub Actions jobs all completed successfully, closing the prior
macOS CI blocker.

The base candidate lock file installed successfully with `npm ci` in all four
base-candidate CI jobs, preserving the declared Node `>=22.22.0` floor. The
base-candidate CI and audit-corrected local critical audit gates exit successfully with zero
critical findings. The historical 2026-07-25 local audit reported 20 transitive
findings (13 high and seven moderate). The scoped override recorded above later
resolved that report without changing the pinned Promptfoo version; validation
details are in the
[Promptfoo integration contract](../../integrations/promptfoo.md#dependency-validation).
The npm registry still reported `0.121.19` as latest on 2026-07-26, so no newer
upstream release was available for a compatible upgrade attempt.
The 2026-07-26 direct audit endpoint twice returned gzip bytes that npm treated
as invalid JSON. Repeating the same critical-level audit through a
credential-stripping loopback decode proxy returned the unchanged registry
report and exited successfully: zero critical, 13 high, and seven moderate;
no package or lock-file mutation occurred.

## Evidence completeness

This table evaluates the record itself, not whether the implementation exists.
Missing fields remain missing; they are not inferred from passing automation.

| Required field | State | Recorded evidence |
| --- | --- | --- |
| Candidate commit | Recorded | `0b703c6dcbbc6841a70c3a290ffae928abda85b8` |
| Execution date | Recorded | 2026-07-25 and audit-amendment reruns on 2026-07-26 |
| Executor and reviewer identity | Partial | The Codex task agent executed the current automated, browser, and runtime checks; no attributable independent human executor or reviewer is recorded |
| OS, Node, and browser versions | Partial | The current local Windows, Node, npm, Git, Chrome/Chromium, and Runtime versions are recorded; exact hosted CI images are not |
| Codex and Claude Code runtime versions | Recorded | Codex CLI `0.145.0`; Claude Code `2.1.218` |
| Golden Path ID and per-path result | Partial | The packet below records every named path, including GP-02A and GP-05A/B, but several required manual paths remain Blocked |
| Per-path duration | Partial | The current isolated Codex lifecycle took 99.120 seconds from `skill.started` to `skill.completed`; the isolated Claude lifecycle reported 873 milliseconds; the isolated Broken-to-Repair path took 48.321 seconds; the timed GP-03 UI path took 3.337 seconds; remaining Golden Paths still lack durations |
| Linked issue or defect reference | Partial | The packet below references the local validation ledger or explicitly records `none observed`; no hosted issue IDs exist for the open manual gates |
| Cleanup and rollback status | Partial | Automated rollback is summarized. All isolated fixtures and servers created by the current checks were stopped and moved to the recycle bin; open manual paths still lack attributable cleanup/rollback attestations |
| Immutable evidence binding | Partial | Current CI is bound to the candidate commit; runtime, performance, browser, and manual evidence is not |

The record is therefore incomplete for general RC promotion.

## Functional evidence

Unless explicitly attributed to current CI or the audit-corrected 2026-07-25
rerun, the detailed evidence below was captured on the pre-commit working tree
based on `446ea71595c0e358abe35d8c4ec9d91ec9089968`. Historical and current local
working-tree results remain useful evidence but are not attestations for an
immutable audit-corrected candidate.

- The audit-corrected suite covers setup preflight including file-as-directory
  rejection, unavailable-vs-read-only truth, and explicit Install confirmation;
  GP-02 `awaiting-verification`; server-local Today plus partial/unavailable
  runtime sources; six-language Command Center explanations; persistent
  consistent Demo truth; explicit protected-audit unlock/error states; ratio
  denominators; Agents evidence/display windows; shared development/production
  local APIs; canonical routes; and the final Managed Decision to Release
  Candidate origin invariant.
- A completed Managed Suite run accepts one exact six-field final Decision.
  Same-value retries reuse it; a changed judgment returns `409` and requires a
  new run. Atomic origin reservation and immutable `originEvaluationRunId`
  prevent one run from creating multiple Release Candidates, including legacy,
  concurrent, retry, and compensation paths. Team retention serializes against
  the complete governance release transaction and cross-process Capability
  writes while preserving origin, latest, quality, and Red Team evaluation
  references.
- A later domain audit found that a direct Candidate proposal could bind
  passing evidence without a final `create-candidate` Decision and then reach
  Stable. The audit-corrected service now keeps such proposals at Candidate,
  requires the Decision for every quality-evidence binding, and keeps the first
  qualifying run as immutable origin. Non-create final Decisions cannot be
  rebound as release evidence. Approval, Canary, Stable, install, rollback,
  legacy reapproval, and pending Canary/Stable forward-release recovery
  revalidate the origin and latest Decisions. Regression tests cover missing or
  conflicting Decisions at binding, after persistence, during previous-Stable
  rollback, and during pending Canary/Stable forward-release recovery. Both v1
  and v2 Registry reads reject duplicate Capability IDs, mismatched
  latest/evidence bindings, and duplicate evaluation run reservations before
  exposing ambiguous Capability state.
- Historical pre-audit evidence says Codex and Claude Code installers were
  previewed and applied against isolated runtime configuration, after which
  both reached `verified`. That run did not record exact Runtime versions or an
  immutable evidence binding.
- The 2026-07-26 audit-amendment rerun used project-scoped hooks and isolated
  temporary data. Codex CLI `0.145.0` emitted post-boundary
  `skill.started` and `skill.completed` for the synthetic
  `skillops-verification` Skill with `explicit_prompt` detection and an
  `unknown` lifecycle outcome. Claude Code `2.1.218` then emitted
  `skill.started` and `skill.completed` for an explicit synthetic slash-Skill
  invocation with `slash_command` detection, confidence `1`, and a reported
  lifecycle duration of 873 milliseconds. The formal recording checker was
  GREEN for both Runtimes. The Claude turn itself failed at a pre-inference
  account gate, so its lifecycle outcome remains `unknown` and is correctly
  excluded from product-success denominators. Project-scoped hooks do not
  satisfy the current user-level `/api/connections` configuration check.
- A separate read-only user-level inspection on 2026-07-26 found both Runtime
  adapters installed with healthy references and an `ok` event source. Codex
  CLI completed a fresh `ponytail-help` invocation and emitted post-boundary
  `skill.started`, `skill.matched`, and `skill.completed`; the formal checker
  returned GREEN and `/api/connections` projected `verified`. Claude Code
  started and emitted user-level activity, but did not advance to Skill use
  before the bounded probe timed out. The formal checker therefore returned
  RED and the connection correctly remained `awaiting-verification`. A
  metadata-only provider probe completed over HTTPS with `401` for both
  supported authentication header modes, locating the remaining failure
  outside the SkillOps adapter. No Runtime configuration value, credential,
  endpoint, local path, response body, or event body was recorded.
- A second isolated Codex configuration intentionally referenced a missing
  adapter file. The API and UI reported `broken`/`degraded` and unhealthy
  reference health without returning the path. Dry-run announced a backup;
  install preserved unrelated fixture metadata, created the backup, and moved
  the connection to `installed`/`awaiting-verification`. **Check installation**
  refreshed the preflight reference to healthy. The clean rerun took 48.321
  seconds, emitted no browser console error, had no horizontal overflow, and
  removed its temporary server and files.
- JSONL regression review found and closed two mutation-order gaps: event clear
  now rejects a partial tail before backup or replacement, and conflict apply
  validates action history before any filesystem copy, rename, or write.
  Adapter hooks also atomically replace an existing error-log entry with a fixed
  redaction marker during an ordinary invocation, without following link
  targets or duplicating sensitive content into a backup.
- Automated smoke exercises loopback serving, SPA routing, privacy validation,
  deterministic Promptfoo evidence, Local Prompt Registry governance and
  rollback, and the local API.
- JSONL tests distinguish a genuinely truncated final object from complete
  syntax or semantic corruption across events, evaluation evidence, governance
  audit, Team collector/audit, and conflict history. Reads expose only a valid
  partial prefix; ordinary append, retention, clear, cleanup, and conflict apply
  fail closed before destructive mutation. The explicit event recovery command
  is backup-first. Event/index rewrites and retention compensation share one
  lock order and preserve concurrent appends.
- Governance tests cover preview token validation, explicit confirmation,
  authoritative refresh, distinct-principal approval, Canary, Stable,
  deprecation, and immutable rollback. Quick Compare stays memory-only; only
  sanitized Managed Suite evidence can support a Release Candidate nomination.
- Historical browser checks used Chrome 150 on Windows. Seven core routes at 390, 768, and
  1366 CSS px had no horizontal page overflow. A 1366-by-900 physical viewport
  at 200% scale had no overflow or critical/serious axe finding. Theme chooser,
  runtime connection, and AI settings dialogs had no critical/serious axe
  finding at 390 px and closed with Escape. The same checks covered the open
  mobile navigation state.
- The audit-corrected rerun now covers all eight canonical direct/refresh
  routes and all seven documented legacy mappings at narrow and desktop widths.
  Axe-core `4.10.3` covers the eight canonical routes at 390, 768, 1366, and a
  683 px 200%-zoom equivalent. The first run exposed three Command Center
  accent labels at 4.43:1; changing the shared label token to the existing
  higher-contrast accent-hover token removed the root cause. The post-fix 32-scenario
  run has zero violations and zero console errors. Independent keyboard and
  human accessibility attestations remain open.
- The static accessibility contract covers all 25 product themes (26 chooser
  options including System), focus styles, 44 px compact touch targets, reduced
  motion, and the 700 px mobile breakpoint.

## Performance and privacy fixture

The endpoint, privacy, responsive-layout, accessibility-tree, browser-network,
and axe fixtures were rerun on the audit-corrected working tree. The current
30-minute soak and UI timing are recorded below after completion. None of the
local evidence is bound to an immutable audit-corrected candidate.

Fixture: seed `skillops-personal-v1`, fixed clock
`2026-07-25T12:00:00.000Z`, UTC, 100,000 normalized synthetic events, 5,000
synthetic definitions, SHA-256
`f7c1650fd8628539c23eca4539aa7aa0a58514d5a252058f58708e9b7496f447`.
Endpoint and soak measurements ran on Windows 11, Node `24.18.0`, x64, an
Intel Core i5-14600KF with 14 physical cores/20 logical processors,
34,188,517,376 bytes RAM, and the host's single ZHITAI TiPlus7100 2 TB device.
The manufacturer identifies TiPlus7100 as a
[PCIe Gen4 x4 NVMe SSD](https://www.ymtc.com/cn/products/43.html), so the host
meets or exceeds the specified 4-core/16-GB/SSD reference environment. Cold
samples use five fresh server processes. Warm samples use 10 warm-ups followed
by 100 sequential requests; p95 is nearest-rank `ceil(0.95 * n)`.

Current audit-corrected working-tree endpoint results:

| Measure | Result | Budget |
| --- | ---: | ---: |
| Command Center warm p95 | 16.21 ms | <= 750 ms |
| Runs page warm p95 | 94.80 ms | <= 500 ms |
| UI `data-received` to next-frame `primary-content-ready` p95 | 114.70 ms | <= 120 ms product target / <= 500 ms release ceiling |
| 30-minute heap growth from five-minute baseline | 217,128 bytes / 0.53% | < 100 MiB and < 20% |

The isolated fixture contains only normalized synthetic metadata. It confirmed
bounded 50-row Runs and Agent-definition pages, a 100,000-event summary, no
browser receipt of the full event array, and no privacy sentinel in
persistence, export, or unrelated APIs.

Current browser network boundaries:

| Request | Returned | Total | Encoded response |
| --- | ---: | ---: | ---: |
| `/api/runs?page=1&pageSize=50` | 50 | 95,000 | 11,737 bytes |
| `/api/agents?tab=definitions&page=1&pageSize=50` | 50 | 5,000 | 89,911 bytes |
| `/api/scan?page=1&pageSize=100` | 46 | 46 | 35,154 bytes |
| `/api/artifacts?page=1&pageSize=100` | 54 | 54 | 106,747 bytes |

All four requests were observed by Chrome DevTools as browser-originated
requests with HTTP 200 responses. None returned an unbounded source array.

The current UI protocol used five warm page loads and 50 sequential measured
production Command Center loads in a foreground Chrome 150 tab at
1366-by-768. The start mark follows response JSON parsing; the end mark is the
next animation frame after the primary content commit. The nearest-rank p95
was 114.70 ms: zero of 50 samples exceeded the 500 ms release ceiling. One raw
sample exceeded 120 ms, while the measured p95 still passed the 120 ms product
budget.

The current memory protocol made one Command Center request every three seconds
for 30 minutes and sampled the server heap every five minutes. It completed the
full protocol: the last 15 minutes grew by 106,424 bytes, the fitted slope was
7,019 bytes/minute, projected tail growth was 105,288 bytes, and the plateau
check passed within both the 5% and 5 MiB limits.

<details>
<summary>Current raw endpoint samples (milliseconds)</summary>

```text
command-center cold: 4130, 4248.09, 3963.88, 3887.24, 4582.02
runs cold: 2006.73, 1974.24, 1886.44, 2385.52, 1726.39
command-center warm: 15.12, 15.58, 16.45, 15.69, 15.68, 15.84, 15.19, 15.78, 15.07, 15.88, 15.09, 15.05, 15.74, 15.16, 15.48, 16.21, 15.28, 15.5, 15.3, 16.23, 16.25, 15.4, 0.61, 14.84, 15.85, 15.63, 15.07, 15.68, 16.37, 16.07, 16.1, 15.41, 15.53, 14.86, 15.23, 15.11, 15.72, 15.68, 15.48, 16.18, 15.21, 15.54, 15.56, 15.75, 15.76, 14.68, 15.78, 15.31, 16.22, 15.14, 16.07, 15.24, 15.22, 15.1, 15.77, 15.97, 14.81, 15.26, 0.55, 15.18, 15.47, 15.37, 15.33, 15.69, 15.85, 15.34, 15.52, 15.16, 15.44, 15.62, 16.17, 14.71, 15.17, 15.46, 15.97, 15.02, 16.01, 16.17, 15.9, 15.27, 15.84, 14.94, 16.1, 15.61, 15.47, 15.69, 15.59, 15.03, 15.58, 16.14, 15.67, 15.92, 15.8, 14.99, 16.16, 0.58, 15.24, 15.66, 15.14, 15.51
runs warm: 59.63, 48, 60.6, 60.19, 51.18, 56.71, 49.47, 46.14, 46.86, 46.16, 46.17, 48.91, 46.29, 50.37, 59.39, 60.39, 50.68, 63.52, 58.73, 65.65, 59.43, 59.01, 66.53, 75.27, 90.41, 74.32, 57.86, 62.85, 60.74, 74.96, 78.49, 89.01, 63.69, 81.45, 92.31, 113.06, 90.34, 95.46, 86.52, 105.36, 103.41, 94.8, 84.87, 95.69, 73.89, 76.55, 90.11, 82.13, 77.01, 75.29, 75, 70.95, 60.62, 63.15, 59.16, 61.85, 65.95, 57.14, 64.55, 58.22, 65.45, 62.25, 70.64, 75.12, 66.95, 83.56, 71.56, 67.75, 61.41, 70.81, 64.33, 72.01, 54.56, 64.66, 65.63, 61.77, 65.34, 59.49, 57.87, 65.74, 57.26, 63, 63.36, 59.98, 61.75, 50.13, 64.79, 54.39, 66.36, 62.97, 57.49, 64.46, 64.38, 61.46, 58.12, 58.23, 66.06, 64.1, 50.8, 56.53
```

</details>

<details>
<summary>Current raw UI commit samples (milliseconds)</summary>

```text
warmups: 69.9, 86.5, 83.8, 85.6, 15.1
measured: 86.6, 104.4, 114.7, 15, 99.1, 90.9, 84.6, 14.8, 81.2, 71.1, 88.8, 13.4, 84.5, 88.6, 112.6, 13.7, 79.4, 102.8, 87.8, 13.8, 85.1, 107.6, 80.5, 13.5, 82.4, 103.8, 91.4, 13.4, 112, 92.4, 87.2, 14.1, 110.9, 110.6, 94.2, 14.3, 89, 91.6, 88.5, 14.8, 114.5, 111.1, 121.9, 14.8, 87.6, 115.9, 80.6, 16.2, 97.1, 109.5
```

</details>

<details>
<summary>Current raw memory trend</summary>

```text
minute 5: 40,713,672 bytes
minute 10: 40,752,824 bytes
minute 15: 40,824,376 bytes
minute 20: 40,847,744 bytes
minute 25: 40,879,432 bytes
minute 30: 40,930,800 bytes
```

</details>

Historical additional evidence:

| Measure | Result | Budget |
| --- | ---: | ---: |
| Command Center warm p95 | 16.38 ms | <= 750 ms |
| Runs page warm p95 | 457.93 ms | <= 500 ms |
| UI `data-received` to next-frame `primary-content-ready` p95 | 8.2 ms | <= 120 ms product target / <= 500 ms release ceiling |
| 30-minute heap growth from five-minute baseline | 720,952 bytes / 1.74% | < 100 MiB and < 20% |

Boundary assertions passed: the browser did not receive the full event array;
Runs and Agent definitions each returned 50 rows; the event summary counted
100,000 records; and the privacy sentinel was absent from persistence, export,
connections, Command Center, Runs, and Agents responses.

The collapsible samples below are the historical pre-commit measurements
retained for comparison; they are not the current working-tree samples above.

<details>
<summary>Raw endpoint samples (milliseconds)</summary>

```text
command-center cold: 1258.38, 1249.68, 1217.1, 1221.2, 1231.71
runs cold: 804.29, 816.83, 815.47, 813.48, 815.39
command-center warm: 16.83, 15.26, 14.48, 15.96, 15.03, 14.94, 0.49, 15.46, 1, 0.82, 13.25, 0.49, 15.3, 15.79, 15.38, 15.12, 16.38, 14.54, 15.28, 16.15, 15.47, 15.62, 15.99, 831.79, 15.14, 15.06, 15.52, 15.32, 15.78, 15.2, 15.23, 15.95, 15.78, 15.33, 15.78, 15.99, 15.05, 15.11, 16.18, 14.78, 15.44, 15.83, 15.09, 15.43, 15.47, 16.05, 15.27, 14.79, 15.67, 16.01, 15.32, 15.51, 15.2, 16.66, 14.39, 16.36, 15.99, 15.38, 14.68, 15.46, 15.47, 15.12, 15.04, 15.44, 15.38, 15.38, 15.54, 14.71, 15.42, 14.86, 15.33, 15.49, 15.84, 15.18, 15.65, 16.11, 15.6, 15.22, 15.41, 14.94, 15.67, 15.61, 15.22, 15.49, 16.01, 0.72, 14.71, 17.28, 2.11, 1.92, 10.01, 15.4, 16.3, 15.08, 15.57, 15.4, 15.93, 15.23, 16.63, 14.25
runs warm: 402.88, 404.26, 413, 407.42, 401.6, 407.86, 424.14, 413.36, 431.57, 416.4, 418.07, 409.9, 441.11, 424.87, 457.93, 449.29, 443.31, 426.53, 428.15, 422.57, 420.96, 414.69, 417.71, 422.69, 424.42, 416.62, 428.37, 412.26, 449.21, 484.67, 2286.98, 727.34, 443.59, 424.02, 441.56, 441.24, 431.7, 414.53, 441.14, 424.95, 456.49, 463.07, 425.44, 435.88, 440.38, 431.92, 444.11, 430.12, 426.71, 407.65, 431.24, 413.87, 413.49, 417.1, 445.9, 442.44, 417.58, 424.38, 432.44, 426.05, 418.77, 422.31, 425.08, 417.45, 404.32, 428.78, 421.7, 419.97, 415.12, 418.79, 432.6, 423.57, 424.22, 440.47, 436.97, 431.78, 436.73, 455.77, 422.12, 446.31, 412.98, 413.39, 414.01, 414.61, 405.99, 430.91, 416.19, 437.31, 445.78, 419.54, 423.22, 430.65, 436.65, 472.39, 415.05, 424.41, 427.18, 420.46, 422.46, 438.57
```

</details>

<details>
<summary>Raw UI commit samples (milliseconds)</summary>

Protocol: five warm page loads, then 50 production page loads at 1366-by-768,
Chrome 150. The start mark follows response JSON parsing; the end mark is the
next animation frame after the primary content commit.

```text
3.6, 3.4, 5.3, 3.5, 2.3, 1.7, 6.9, 5.3, 4.8, 4.7, 5, 7.3, 6.8, 6.2, 7.1, 7.4, 3.5, 3.5, 2.8, 4.8, 2.2, 7, 6.3, 8.4, 2.9, 6, 7.5, 5.1, 7.5, 8.2, 5.7, 1.5, 3.9, 6.4, 4.2, 2.2, 2.2, 2.2, 6.3, 8, 4.5, 4.3, 3.4, 7.2, 3, 7.1, 3.3, 8.8, 3.9, 7.7
```

</details>

<details>
<summary>Raw memory trend</summary>

```text
minute 5.01: 41,376,776 bytes
minute 10.01: 41,562,712 bytes
minute 15.01: 41,932,680 bytes
minute 20.01: 41,951,312 bytes
minute 25.01: 42,012,088 bytes
minute 30 final: 42,097,728 bytes
```

</details>

## Golden Path packet status

Shared packet fields: version `v0.3.2-rc.1`, base commit
`0b703c6dcbbc6841a70c3a290ffae928abda85b8`, audit-amendment date
2026-07-26, automated executor **Codex task agent**, Windows
`10.0.26200`, Node `24.18.0`, npm `11.5.2`, Git
`2.52.0.windows.1`, Chrome/Chromium `150.0.0.0`, Codex CLI `0.145.0`,
and Claude Code `2.1.218`. Automated tests and browser work used synthetic or
isolated data; no default repository event data was cleared or rewritten.
This table is a status packet, not an independent human attestation.

| Path | RC result | Current evidence | Duration | Tracking | Cleanup / rollback |
| --- | --- | --- | --- | --- | --- |
| GP-01 | **Blocked** | Isolated Codex and Claude Code both emitted strict post-boundary `skill.started` and `skill.completed`; the isolated formal checker was GREEN for both. The final real user-level checker was GREEN for Codex and RED for Claude Code, matching the `verified` / `awaiting-verification` projection. Claude still has no qualifying user-level Skill lifecycle, while complete setup and keyboard paths are unsigned | Codex lifecycle 99.120 s; isolated Claude lifecycle 0.873 s | `VAL-002`, `VAL-004` | No default Runtime data was mutated; the isolated Runtime fixture was moved to the recycle bin after the test servers stopped |
| GP-02 | Pass — automated | Discovery-only configuration stays `awaiting-verification`; UI requests a real invocation | Covered by the full suite; not separately timed | none observed | Test-owned isolated stores; no user/default data mutation |
| GP-02A | Pass — automated | Boundary tests reject pre-install lifecycle evidence after the configuration timestamp changes | Covered by the full suite; not separately timed | none observed | Test-owned isolated stores; no user/default data mutation |
| Broken→Repair | **Working-tree pass; immutable binding open** | An isolated missing adapter reference produced Broken/degraded and sanitized unhealthy preflight; dry-run, backup-first repair, preservation of unrelated settings, refreshed healthy preflight, and awaiting-verification truth all passed | 48.321 s | `VAL-002` | Isolated server stopped, port released, and fixture removed |
| GP-03 | **Working-tree timed pass; immutable/independent gate open** | From Command Center, the failed-run action opened filtered Activity and the synthetic Run detail with failed status, duration, Skill/version/project, and started/failed timeline. The timed UI fixture injected prompt/output/raw-error sentinels and rendered none; automated privacy contracts separately cover task/source and all forbidden classes | 3.337 s | `VAL-002`, `VAL-007` | No user data was mutated; the isolated server was stopped and its fixture was moved to the recycle bin |
| GP-04 | **Blocked** | Preview/diff/confirm/rescan/undo behavior passes automation; independent keyboard path is unsigned | Automated portion not separately timed | `VAL-004`, `VAL-007` | Automated undo/rollback passes; no manual source mutation |
| GP-05 | **Blocked** | Managed Suite, Decision, Candidate-to-Release-Candidate provenance, approval, Canary, Stable, and prior-Stable contracts pass automation; missing origin/latest Decisions and non-create final Decisions fail closed in direct actions and pending Canary/Stable forward-release recovery. Independent Reviewer release and keyboard paths are unsigned | Automated portion not separately timed | `VAL-004`, `VAL-007` | Automated rollback passes; no manual release mutation |
| GP-05A | **Blocked for manual gate** | Distinct-principal and self-approval rejection pass automation; manual single-identity UI confirmation is absent | Automated portion not separately timed | `VAL-007` | Rejected mutation leaves governed state unchanged |
| GP-05B | Pass — automated | Quick Compare remains memory-only and cannot create governed Evidence or Capability state | Covered by the full suite; not separately timed | none observed | Session-only state is discarded |
| GP-06 | Pass — automated | Insufficient sample/coverage fails the gate and does not recommend promotion | Covered by the full suite; not separately timed | none observed | No release mutation occurs |
| GP-07 | **Blocked** | Preview, confirmation, authoritative reread, failure truth, and rollback pass automation; independent keyboard path is unsigned | Automated portion not separately timed | `VAL-004`, `VAL-007` | Automated rollback/recovery passes |
| GP-08 | Pass — automated | Core local pages and historical evidence remain usable without an AI Provider; provider-required actions expose setup | Covered by the full suite; not separately timed | none observed | No provider credential was created |
| GP-09 | Pass — automated | Genuine truncated tails expose valid-prefix Partial state. Ordinary writes, event clear, conflict apply, and retention reject partial/corrupt state before destructive mutation; explicit event recovery remains backup-first | Covered by the full suite; not separately timed | none observed | Original source is preserved on rejected mutations; explicit recovery retains its source backup |
| GP-10 | Pass — automated | Persistence and response allowlists reject prompt, task, source, output, raw-error, and credential sentinels. Codex and Claude hooks atomically replace existing diagnostic entries with fixed safe markers without writing through hardlink targets | Covered by the full suite; not separately timed | none observed | Synthetic stores only; linked targets remain byte-identical and no sensitive diagnostic backup is created; artifact check reports sanitized output |

## Release gate assessment

| Gate group | Status | Evidence |
| --- | --- | --- |
| Codex and Claude Code first connection | **Codex user-level lifecycle pass; Claude/manual/immutable gate open** | Exact versions are recorded. Isolated current-tree Codex and Claude Code runs both produced strict post-boundary `skill.started` and `skill.completed`, and the isolated checker was GREEN for each. The final real user-level checker returned GREEN for Codex and RED for Claude Code, matching the `verified` / `awaiting-verification` connection projection. The isolated Claude turn had an `unknown` outcome after an account-gate failure, not a product success |
| Reinstall/repair boundary; discovery; unknown outcome | **Working-tree agent-driven path pass; required human/immutable gate open** | The isolated 48.321-second Broken-to-Repair run passed backup-first repair, unrelated-setting preservation, preflight refresh, and correct post-install `awaiting-verification`; it is not an independent human attestation and is not yet bound to an immutable audit-corrected candidate |
| Setup preflight contract | **Working-tree agent-driven path pass; required human/immutable gate open** | `/api/setup/preflight` is metadata-only; tests distinguish probe unavailable from a completed negative result (including built-in invalid-path failures), reject a writable regular file as a data directory, validate the full client response shape, display unavailable separately from read-only, treat an unconfigured or broken adapter as installable, lock failed prerequisites, and reveal Install only after explicit dry-run review. The isolated repair run confirmed unhealthy-to-healthy refresh without path disclosure, but it is not an independent human attestation |
| Governance audit authentication | **Working-tree implementation pass; immutable evidence open** | Protected lock, global audit, and capability-scoped audit reads require a configured Bearer principal; the UI explicitly unlocks capability audit with a transient token and keeps `403` distinct from empty. Current immutable/manual evidence is still missing |
| Outcome denominator and missing cost | **Implementation pass** | Command Center returns known-outcome and reported-cost numerator/denominator fields, explicit definitions, and unavailable states when the denominator is zero |
| Command Center action bound | **Working-tree implementation pass; immutable evidence open** | Audit-corrected projection and UI tests cap ranked actions at three, use server-local Today, preserve known partial metrics, refuse unavailable event or connection facts as zero/empty claims, localize explanations, preserve Demo labeling, and require reason, evidence, impact, and one CTA |
| Agent truth surface and recency | **Working-tree implementation pass; immutable evidence open** | Fabricated Online, Current Task, relationship, and context-free score claims are absent; fixed readiness evidence recency is independent from the 7/14/30-day display filter |
| Activity server pagination | Pass | Audit-corrected working-tree `/api/runs` tests pass; the historical fixture returned bounded pages |
| Universal list pagination and freshness | **Working-tree pass; immutable evidence open** | Events, connections, Runs, Agents, Scan, Artifact Registry, Suites/History/Cases, Capabilities/audit, Team catalog/queues/audit, Prompt Registry/branches, and PromptHub project/drift reads are bounded, deterministically ordered, and expose `generatedAt` or revision. Cross-page Candidate recovery, release deep links, previous Stable, Prompt branch navigation, serialized Managed History paging, and event-query validation before `304` are covered by current tests |
| Quick Compare memory-only draft | Pass | Audit-corrected working-tree tests prove no persisted result, Capability, governed run ID, Evidence Hash, or Gate |
| Managed Suite persistence and Release Candidate nomination | **Implementation pass; immutable evidence open** | Canonical and compatibility Decision routes, exact request/public schemas, idempotent retry, one-final-Decision, Candidate proposal confinement, Decision-gated origin and evidence binding, one-origin-run/one-Release-Candidate, direct-action and pending Canary/Stable forward-release provenance revalidation, migration, race, legacy, rollback compensation, and atomic retention-preservation tests pass |
| Release preview, confirmation, refresh, and rollback | **Implementation pass; manual gate open** | Audit-corrected working-tree governance tests and smoke cover the implementation; independent release review is not recorded |
| Persistence/response privacy sentinel | **Working-tree pass; immutable evidence open** | The audit-corrected 100k fixture found no forbidden persistence or response exposure; no immutable audit-corrected candidate binding exists |
| 100k/5k performance and 30-minute memory | **Audit-corrected pre-commit protocol pass; immutable binding open** | The audit-corrected pre-commit candidate tree passed the endpoint, privacy, and 30-minute memory protocol: Command Center/Runs p95 were 16.21/94.80 ms and heap growth was 217,128 bytes / 0.53%. The separate final browser rerun had no application console errors or unbounded app requests; the recorded 1366×768 UI timing remains a prior working-tree measurement. Formal RC closure still requires immutable-candidate binding or an attributable equivalence attestation |
| 390/768/1366 responsive routes and axe | **Working-tree automated pass; immutable/independent gate open** | All eight canonical routes and seven legacy mappings passed direct/refresh checks at narrow and desktop widths. Axe-core `4.10.3` reported zero violations across eight canonical routes and four viewports after the shared contrast fix. The result is neither immutable-candidate-bound nor an independent human attestation |
| GP-01, GP-04, GP-05, and GP-07 keyboard-only paths | **Blocked** | Automated focus/Escape behavior passed, but independent end-to-end keyboard sign-off is absent |
| P0 Golden Path coverage | **Implementation pass; evidence packet incomplete** | Every named path now has an explicit packet row, and GP-03 has a timed working-tree UI result; blocked manual paths, remaining per-path durations, and independent review remain open |
| Ubuntu/macOS/Windows Node 22.22 plus Ubuntu Node 24 | **Base candidate pass; corrected-candidate gate open** | Base commit `0b703c6dcbbc6841a70c3a290ffae928abda85b8` passed all four jobs in [run 30145860716](https://github.com/Gjts/skillops/actions/runs/30145860716); the audit-corrected candidate is the commit containing this record, but its four-job matrix has not yet run |
| Platform skips | Pass | The historical Windows POSIX mode skip is explained and the corresponding Linux test passed |
| P0 Blocker / P1 Critical defect count | **Working-tree implementation review pass; independent gate open** | Automated checks and a final agentic diff review found no unresolved code-level P0 Blocker or P1 Critical; the required attributable independent release review is still not recorded |
| `npm test`, build, smoke, docs, artifacts, critical audit | Pass — local working tree | All six command groups passed in the base-candidate CI matrix and the audit-corrected local rerun; the latest full Windows test run had 78 files, 799 passed tests, and one skip. Route-level lazy loading plus a separate i18n catalog reduced the largest production JavaScript chunk to 336.46 kB and removed the greater-than-500-KiB advisory. The current dependency mitigation still lacks immutable candidate and four-platform CI binding |
| Versioned RC evidence record | **Incomplete** | This amendment is bound by the commit containing it; required independent executor/reviewer and per-path fields remain incomplete |
| Five-person VFV and decision-loop sample | **Blocked** | No qualifying human sample was available; product remains Limited Preview |
| README, User Guide, Privacy, and Architecture consistency | **Candidate pass; CI evidence open** | English/Chinese READMEs plus product, architecture, testing, and security docs in the commit containing this record match the implemented IA and contracts; local link validation passes, while corrected-candidate CI is pending |

## Known limitations

- Project-scoped Codex or Claude hooks can emit real events, but the connection
  projection inspects user-level Runtime configuration only. The current
  user-level Codex configuration has qualifying evidence; Claude Code still
  requires a qualifying post-boundary user-level Skill lifecycle.

## Release blockers

The implemented P0 product surface is suitable for continued Limited Preview,
but general RC promotion remains blocked:

1. The audit-corrected implementation and this amended record are bound by the
   commit containing this record. Its four-job matrix is pending; real-runtime,
   performance, browser, axe, and manual evidence must be rerun against that
   immutable candidate or receive a recorded equivalence attestation.
2. The evidence packet must name its independent executor/reviewer and record
   every Golden Path result/duration, issue reference, and cleanup/rollback
   status. Exact local Runtime versions are now recorded.
3. The required five-person first-use validation sample has not been run.
4. Independent keyboard-only sign-off and P0 Blocker / P1 Critical defect review
   have not been recorded.
The base-candidate CI matrix closes the previous macOS blocker. Do not
promote Limited Preview to general RC until every remaining blocker is closed.
